@preinc "inc.gear"		//	preprocess file & include

/*
	C main function
		argc - input arguments count
		argv - input arguments
*/
int main(int argc, char** argv) {
	int result = 1 + 2 - 3 * 4 / 5 + (int)main;
	return result;								//	return value from `main(int, char**)`
	@save {Hello, }	@save{World!	// huh :D}
}